<idea-plugin version="2" url="https://github.com/wimsymons/intellij-ppimport">
	<id>be.mavicon.intellij.ppimport</id>
	<name>Polopoly Importer</name>
	<version>0.9</version>
	<vendor email="wim.symons@gmail.com">Wim Symons</vendor>

	<description><![CDATA[
Plugin to import content XML to Polopoly, or any service that accepts HTTP PUT requests for that matter.
This version keeps state on application-level, not on project level.
]]></description>

	<change-notes><![CDATA[<pre>
0.9 Added ability to find and replace strings in the imported data.
0.8 Fixed the import progress.
    Fixed the import of files in the VCS Changes.
0.7 Added ability to Cancel the background task.
    Added import actions to the VCS Changes popup menu.
0.6 Restore _Import finished_ message.
		Do progress update for a single file.
0.5 Show progress dialog.
		Changed _Import successful_ to _Import finished_.
0.4 Recurse files in alphabetic order.
		Less notifications about successful imports.
0.3 Do imports asynchronously to avoid hanging IDE.
0.2 Fixed configuration storage.
0.1 Initial release.
</pre>]]></change-notes>

	<idea-version since-build="107.105"/>

	<application-components>
		<component>
			<implementation-class>be.mavicon.intellij.ppimport.PPImportPlugin</implementation-class>
		</component>
	</application-components>

	<actions>
		<group id="PolopolyImportPluginGroup" popup="true" text="Polopoly Import" description="Import content">
			<add-to-group group-id="ProjectViewPopupMenu" anchor="last"/>
			<add-to-group group-id="StructureViewPopupMenu" anchor="last"/>
			<add-to-group group-id="EditorPopupMenu" anchor="last"/>
			<add-to-group group-id="ChangesViewPopupMenu" anchor="last"/>
		</group>
	</actions>

</idea-plugin>